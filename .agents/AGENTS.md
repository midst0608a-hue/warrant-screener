# Project Customization Rules

- Whenever the user asks to "sync to GitHub" or "更新到 GitHub", automatically perform `git add .`, `git commit -m "<descriptive message>"`, and `git push origin master:main` in `c:\Users\midst\OneDrive\桌面\youanalysis_notes\不動產權全能工作站` without requiring manual steps.
